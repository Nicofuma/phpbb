# phpBB's config file (This line is needed because of the packager)
security:
    providers:
        phpbb_db:
            id: security.user_provider.phpbb_database

        phpbb_front:
            chain:
                providers:
                    - phpbb_db

    firewalls:
        dev:
            pattern:  ^/(_(profiler|wdt)|assets|css|images|js)/
            security: false

        main:
            provider: phpbb_front
            pattern: ^/
            logout:
                path:   /user/logout
                target: /
            anonymous: true
            switch_user: true
            guard:
                authenticators:
                    - security.guard.form_login_authenticator
                entry_point: security.guard.form_login_authenticator

    access_control:
        - { path: ^/adm, roles: IS_REAUTHENTICATED }
        - { path: ^/, roles: IS_AUTHENTICATED_ANONYMOUSLY }

core:
    session:
        # handler_id set to null will use default session handler from php.ini
        handler_id:  ~
        use_cookies: true
        cookie_httponly: true
       # cookie_secure: false
